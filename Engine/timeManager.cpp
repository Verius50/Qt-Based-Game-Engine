#include "timeManager.h"

//bool TimeManager::eventFilter(QObject* obj, QEvent* event)
//{
//	if (event->type() == QEvent::Timer) {
//		emit timerEnd(static_cast<QTimerEvent*>(event)->timerId());
//	}
//	return false;
//}
